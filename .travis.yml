language: "node_js"
node_js:
 - 10
jobs:
 include:
  - stage: Deploy to Firebase
    script: "yarn deploy-web"
  - stage: Deploy to Heroku
    script: skip
    deploy:
     provider: heroku
     api_key: "$HEROKU_KEY"

